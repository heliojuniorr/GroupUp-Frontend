import styled from "styled-components";

export const Container = styled.main`
    background: #eee;
    padding: 1rem 2rem 1rem 0rem;
    overflow-y: auto;

    li {
        list-style: none;
        margin-left: 0;
    }

    .default-message {
        padding: 1rem;
    }

    .filter-field {
        margin-left: 1rem;
    }
`